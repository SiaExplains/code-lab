import React from 'react';
import './PizzaImage.css';
import PizzaImage from '../../asstes/pizza.jpg';

const pizzaImage = (props) => {
    return (
        <div className={style.PizzaImage}>
            <img src={PizzaImage} className={style. } />
        </div>
    );
};

export default pizzaImage;
